#include<stdio.h>
#include <math.h>
int main(){
    // Part-1: EXCERCISE 1: Ask the user for the char and find whether its 
    //upper case or lower case alphabet
    
    // char ch;
    // printf("enter one character");
    // scanf("%c", &ch);
    
    // if (ch>=65 && ch<=90 )
    // {
    //     printf("the character you entered falls in upper case");
    // }
    // else if(ch<=122 && ch>=97)
    // {
    //     printf("the char you entered falls in lower case");
    // }
    // else
    // printf("you haven't entered the character");
    // // char a='z';
    // // printf("%d",a);

    // Part 2:    Math.h---> power, sqrt, cos, sin.. 
    // float a=9,power=2;

    // printf("%f",pow(a,2));
    // printf("%f",sqrt(a));
    // return 0;

    // PART-3:   Assignment operator: =, +=,*=,-=,/=,...... 
    // int a=5;
    // a+=4;  //a=a+4;
    // a-=4;   //a=a-4
    //    // a=a&b
    // a/=5 //a=a/5

    //H.W  Take ax^2+bx+c=0 , a,b,c input from user, check if b^2-4ac <0(imaginary)
    //,=0(only one solution),>0 (two solution)
    //(-b+-sqrt(b^2-4ac))/(2a)  
    
    // part -4:order precedence and assocuiativity
    // float num;
    
    // // num=7/2.0*7.0;
    // int a=3 , b=2;
    // num=2.0/3*a%b;

    // printf("%f",num);
    // int a=1;
}