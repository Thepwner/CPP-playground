#include<stdio.h>

int main(){
    int a=9,res;
    int b=10;
    //part-1
    /* 
        conclusion: % operator int->int
            int-> int --> int
            int-> float--> float    
    */
    // printf(" the remainder is :%d",a%b);
    // printf("\nthe sum of is :%f", a + b);
    // printf("\nithe multiplication is :%f", a*b);
    // printf("\nthe substraction is :%f", b-a);
    // printf("\nthe division is %f", b/a);
    
    // PArt-2
    // res=a--;
    // printf("res = %d::: a=%d",res,a);

    // res=--a;
    // printf("res=%d:::::a=%d",res,a);

    //part-3-Relational operator

    // printf(" is a equal to b: %d",a==b);
    // printf("\nis a greater than b ?:%d", a>b);
    // printf("\nis a smaller than b ?:%d", a<b);
    // printf("\nis a smaller or equals b ?:%d", a<=b);
    // printf("\nis a greater equals b ?:%d", a>=b);
    // printf("\nis a equals b ?:%d", a==b);
    // printf("\na is not equals to b:%d", a!=b);


    //part-4 Logical operator    

    // if ((a<b) || (printf("we are checking ande"))){
    //     printf("WE are in");
    // }


    // typedef itn it;
   // #define pi 3.14

//    pre processing, post processing operators 
//    like a++, a--, ++s, --a 
     return 0;
}